# Raw candump captures — large, regenerable with nmea_capture.py
captures/

# Python bytecode
__pycache__/
*.pyc

# Claude Code machine-local settings
.claude/settings.local.json
camera-relay/config.json

# Runtime REC accounting is boat state, never a deployable source artifact.
dbus-recbms/solar-control-state.json
dbus-recbms/.energy-*
